If you use global.dt_steady in every step check to determine actions, then you can use a simple check in
obj_steadydt and change the "scale" variable to 0, which will pause the game(if it is set up correctly)
You can also use this to slow down or speed up the game